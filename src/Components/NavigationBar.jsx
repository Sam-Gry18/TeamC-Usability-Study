//import {NavBar,Toolbar, makeStyles} from "@material-ui/core";
import Box from "@mui/material/Box";
import BottomNavigation from "@mui/material/BottomNavigation";
import BottomNavigationAction from "@mui/material/BottomNavigationAction";
import { theme } from "../theme";
import React from 'react';

/* const Stack = createStackNavigator();

function Section1StackScreen(){
    return(
        <Stack.Navigator>
            <Stack.Screen name="Section 1" component={Section1}/>
        </Stack.Navigator>
    )
}

function Section2StackScreen(){
    return(
        <Stack.Navigator>
            <Stack.Screen name="Next" component={Section2}/>
        </Stack.Navigator>
    )
}

const Tab = createBottomTabNavigator();

export default function Navigation(props){
    return(
        <NavigationContainer>
            <Tab.Navigator>
                <Tab.Screen name ="Section1" component={Section1StackScreen}/>
                <Tab.Screen name ="Section2" component={Section2StackScreen}/>
            </Tab.Navigator>
        </NavigationContainer>
    )
} */
/* export default function BottomNavigation(){
    const [value, setValue] = React.useState(0);

    return (
        <Box sx={{ width: 500 }}>
          <BottomNavigation
            showLabels
            value={value}
            onChange={(event, newValue) => {
              setValue(newValue);
            }}
          >
            <BottomNavigationAction label="Recents" icon={<RestoreIcon />} />
            <BottomNavigationAction label="Favorites" icon={<FavoriteIcon />} />
            <BottomNavigationAction label="Nearby" icon={<LocationOnIcon />} />
          </BottomNavigation>
        </Box>
      );
} */

/* const useStyles = makeStyles((theme) => ({}));

const Navbar = () => {
    const classes = useStyles();
    return (
        <NavBar>
            <Toolbar>
            </Toolbar>
        </NavBar>
    );
};

export default Navbar; */